#include<stdio.h>
void main() 
{
     int num, i, j, flag=0, result;
     scanf("%d", &num) ;
     for(i=2;i<=num/2;i++) 
     {
          if(num%i==0) 
               flag=1;
               break;
     }
result =(flag==1)?printf("not prime") :printf(" prime") ;
 }
